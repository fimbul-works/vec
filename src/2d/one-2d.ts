import type { ArrayVector2D } from "./types.js";

/**
 * Creates a 2D vector with all components set to 1.0.
 * @returns {ArrayVector2D} Vector `[1, 1]`
 */
export const one2D = (): ArrayVector2D => [1, 1];
