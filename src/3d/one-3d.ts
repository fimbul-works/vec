import type { ArrayVector3D } from "./types.js";

/**
 * Creates a 3D vector with all components to to 1.0.
 * @returns {ArrayVector3D} Vector `[1, 1, 1]`
 */
export const one3D = (): ArrayVector3D => [1, 1, 1];
